spring:
  profiles:
    active:
    - dev
    
---

server:
  port: 8201
spring:
  profiles: dev
  application:
    name: microservicecloud-config-client
    
eureak:
  client:
    service-url:
      defaultZone: http://eureak-dev.com:7001/eureka/
      
---

server:
  port: 8202
spring:
  profiles: test
  application:
    name: microservicecloud-config-client
eureak:
  client:
    service-url:
      defaultZone: http://eureak-test.com:7001/eureak/